# dbt/

**T** of ELT — *Transform*.

The dbt project goes here. dbt runs SQL `SELECT` statements inside Postgres to turn the
`raw` tables into clean, joined tables (e.g. prices joined to companies by CIK, market cap).

Created later with `dbt init` once data is loading.
